# Fly.io config for the enter-giveaway-inator worker (no web service; it just runs the
# watch loop and pushes to Discord + ntfy). If the app name is taken, change it and rerun.
app = "enter-giveaway-inator"
primary_region = "ord"

[build]

# Persistent volume so seen.db (the de-dupe store) survives restarts and deploys.
[mounts]
source = "giveawayinator_data"
destination = "/data"

# 2 scans/day at 10:00 and 17:00 US Central (DST-aware) to fit the Apify free tier.
[processes]
app = "giveawayinator --at 10:00,17:00 --tz America/Chicago -c config.cloud.toml"

[[vm]]
size = "shared-cpu-1x"
memory = "1024mb"  # headless Chromium needs headroom; 512mb OOM-kills the browser
